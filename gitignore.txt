# Этот файл (.gitignore) ДОЛЖЕН быть на GitHub.
# Он указывает, какие ДРУГИЕ файлы и папки НЕ НУЖНО туда отправлять.

# Секреты и ключи (их НЕЛЬЗЯ выкладывать в открытый доступ)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Зависимости (слишком тяжелые, устанавливаются через npm install)
node_modules/

# Результаты сборки
dist/
dist-ssr/
build/
.vercel/

# Логи и кэш
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.cache/
.parcel-cache/

# Системные файлы
.DS_Store
Thumbs.db
*.local
